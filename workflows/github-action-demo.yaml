name: @Github /Actions Demo
run-name: ${{ github.actor }} is testing out Github Actions
on: {push}
jobs:
  Explore-Gihub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: echo "The job was automatically triggered by a ${{ github.event_name }} event."